AVM contract

Use IDE to load your contract project by POM

Or directly use `mvn compile` to build the contracct and then use `mvn test` to run the test

The avm contract deploy jar will be generated after run `mvn test`
