package org.aion.maven.state;


/**
 * A wrapper around a reference to an IPFS-resident entity.
 * We may replace this with some native concept, later on.
 */
public class IpfsReference {
    // TODO:  Implement once the IPFS support is added.
}
